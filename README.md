# 🌤️ Weather API Fetcher Simulator (Python Threading)

ผู้จัดทำ: นายชินดนัย อภิบุญญา (รหัสนักศึกษา 6810110570)

โปรแกรมจำลองการดึงข้อมูลสภาพอากาศจากหลายเมืองพร้อมกัน เพื่อศึกษาและทดลองการทำงานของ **Python Threading**

## 🤔 ทำไมถึงเลือกทำเรื่องนี้?
การดึงข้อมูลจากภายนอก (เช่น API ของเซิร์ฟเวอร์ หรือ Database) เป็นสิ่งที่พบเจอได้บ่อยมากในการพัฒนาซอฟต์แวร์และเว็บไซต์จริง ปัญหาคืองานประเภทนี้มักจะมี "ระยะเวลารอคอย" (Network Latency) โปรแกรมนี้จึงถูกสร้างขึ้นมาเพื่อจำลองสถานการณ์ดังกล่าวให้เห็นภาพชัดเจนที่สุดว่า ความล่าช้าจากการรอเครือข่ายส่งผลต่อระยะเวลาทำงานรวมของโปรแกรมอย่างไร

## ⚙️ ทำไมถึงใช้เครื่องมือ Threading ในไอเดียนี้?
งานดึงข้อมูลจากอินเทอร์เน็ตจัดเป็นงานประเภท **I/O Bound** (Input/Output Bound) ซึ่ง CPU แทบไม่ได้ออกแรงคำนวณอะไรเลย แต่ต้องเสียเวลานั่งรอข้อมูลเฉยๆ 
การใช้ **Threading** จึงตอบโจทย์ที่สุด เพราะมันอนุญาตให้โปรแกรม "สลับ" ไปทำงานอื่น (เช่น ไปสั่งดึงข้อมูลของเมืองถัดไป) ในระหว่างที่กำลังรอข้อมูลของเมืองแรกอยู่ได้ ทำให้ไม่ต้องรอคิวทำงานทีละอัน (Sequential) 

## 💡 ไอเดียนี้ทำอะไร และได้อะไร?
* **สิ่งที่ทำ:** โปรแกรมจะจำลองการขอข้อมูลสภาพอากาศจาก 5 เมือง (เช่น Bangkok, Tokyo, London, ฯลฯ) โดยใช้ `time.sleep()` เป็นตัวแทนของเวลาที่ใช้ในการรอเน็ตเวิร์ค
* **สิ่งที่ได้:** ได้เห็นข้อแตกต่างของเวลาอย่างชัดเจน หากรันทีละเมืองอาจใช้เวลารวม 10-15 วินาที แต่เมื่อใช้ Threading เวลาที่ใช้รันโปรแกรมจนจบจะเท่ากับเมืองที่ใช้เวลาโหลดนานที่สุดเพียงเมืองเดียวเท่านั้น (ประมาณ 2-3 วินาที)

## 🔄 วิธีการทำงานของโปรแกรม
1. โปรแกรมกำหนดรายชื่อเมืองเป้าหมายไว้ในรูปแบบ List
2. สร้าง Thread แยกสำหรับแต่ละเมือง โดยมอบหมายเป้าหมาย (Target) ให้ไปรันฟังก์ชัน `fetch_weather_data`
3. สั่งให้ทุก Thread เริ่มทำงานพร้อมกันด้วยคำสั่ง `.start()`
4. โปรแกรมหลักจะรอให้ทุก Thread ทำงานจนเสร็จสมบูรณ์ผ่านคำสั่ง `.join()`
5. สรุปผลและแสดงระยะเวลารวมทั้งหมดที่ใช้ไป

## 🚀 การสอนใช้งาน (วิธีรันโปรแกรม)
1. ติดตั้ง Python (เวอร์ชัน 3.x) ลงในเครื่องให้เรียบร้อย
2. เปิด Terminal หรือ Command Prompt แล้วเข้าไปที่โฟลเดอร์ของโปรเจกต์นี้
3. พิมพ์คำสั่งเพื่อรันโปรแกรม : python main.py

## 📂 โครงสร้างไฟล์ (Project Structure)
```text
weather_api_simulator/
│
├── .gitignore       # ไฟล์สำหรับละเว้นไฟล์ระบบที่ไม่ต้องการให้ Git ติดตาม
├── README.md        # ไฟล์อธิบายรายละเอียดโปรเจกต์และวิธีการใช้งาน
└── main.py          # ไฟล์ Source Code หลักของโปรแกรม